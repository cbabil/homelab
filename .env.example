# Homelab Assistant Environment Variables
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!

# =============================================================================
# REQUIRED - These must be set before starting the application
# =============================================================================

# JWT Secret Key for authentication tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=

# Master password for encrypting server credentials
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
HOMELAB_MASTER_PASSWORD=

# Salt for key derivation (must be unique per installation)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(16))"
HOMELAB_SALT=

# =============================================================================
# OPTIONAL - Override defaults as needed
# =============================================================================

# Environment mode: development or production
# In production: strict SSH host key checking, generic error messages
PYTHON_ENV=production

# CORS allowed origins (comma-separated)
# Default: http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003
# Example for production: https://homelab.example.com
ALLOWED_ORIGINS=

# Data directory for SQLite database and other files
# Default: ./data (relative to backend/)
DATA_DIRECTORY=

# Log level: DEBUG, INFO, WARNING, ERROR
MCP_LOG_LEVEL=INFO
