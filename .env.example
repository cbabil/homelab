# Database Configuration
MYSQL_USER=semaphore
MYSQL_PASSWORD=change_me
MYSQL_DATABASE=semaphore
MYSQL_ROOT_PASSWORD=change_me

# Semaphore Configuration
SEMAPHORE_ADMIN=admin
SEMAPHORE_ADMIN_PASSWORD=change_me
SEMAPHORE_ADMIN_NAME=Administrator
SEMAPHORE_ADMIN_EMAIL=admin@example.com
SEMAPHORE_ACCESS_KEY=generate_secure_key_here

# Pi-hole Configuration
PIHOLE_PASSWORD=change_me

# User/Group IDs for container permissions
# These should match your host user's UID/GID
# Find these values by running: id -u and id -g
UID=1000
GID=1000

# Instructions:
# 1. Copy this file to .env
# 2. Generate a secure access key: head -c32 /dev/urandom | base64
# 3. Replace all passwords with secure values
# 4. Update UID/GID to match your user (use id -u and id -g commands)
# 5. Update email and domain settings as needed 